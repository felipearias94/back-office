import { HigherFontDirective } from './higher-font.directive';

describe('HigherFontDirective', () => {
  it('should create an instance', () => {
    const directive = new HigherFontDirective();
    expect(directive).toBeTruthy();
  });
});
